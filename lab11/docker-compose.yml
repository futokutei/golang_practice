services:
  app:
    image: lab11:latest
    container_name: goApp
    ports: ["3000:3000"]
    depends_on:
      - db
  db:
    image: postgres:15-alpine
    container_name: db
    environment:
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: contacts_db
    ports: [ "5432:5432" ]